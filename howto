#for compile with logging feature
	#compile
	make cmacros="-D WITH_LOGGING"
	#install
	sudo make install cmacros="-D WITH_LOGGING"


#without logging feature
	#compile
	make
	#install
	sudo make install
